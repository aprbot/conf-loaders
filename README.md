
```sh
pip install conf-loaders
```

# Changelog

## 0.4.0

* perform absolute path resolution recursively and not only in first level values
* if variable starts with `!`, it will not be resolved as absolute path

